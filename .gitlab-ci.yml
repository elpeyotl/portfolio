variables:
  HOST: "peyotedesign.ch.arvandus.ch-meta.net"
  USERNAME: "peyotler"
  PASSWORD: "M3tall1cA"

deploy:
  script:
    - apt-get update -qq && apt-get install -y -qq lftp
    - lftp -c "set ftp:ssl-allow no; open -u $USERNAME,$PASSWORD $HOST; mirror -Rnev dist/ ./public_html/test --ignore-time --parallel=10 --exclude-glob .git* --exclude .git/"
  only:
    - master